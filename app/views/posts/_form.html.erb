<%= simple_form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row"><div class="large-4 columns"><%= f.input :title %></div></div>
  <div class="row"><div class="large-8 columns"><%= f.input :body %></div></div>

  <div class="actions row">
    <div class="large-4 columns"><%= f.button :submit, class: "tiny radius"  %>
</div>  </div>
<% end %>
