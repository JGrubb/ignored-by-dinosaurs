<div class="row">
  <% @posts.each do |post| %>
    <div class="post">
      <h2><%= post.title %></h2>
      <div><%= post.body %></div>
    </div>
  <% end %>
  <%= link_to 'New Post', new_post_path, class: "tiny radius button" %>
</div>

